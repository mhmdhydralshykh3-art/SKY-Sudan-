import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.skysudan.app",
  appName: "Sky Sudan",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
